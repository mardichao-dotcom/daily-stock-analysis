# Stage 8 上線後待辦

> 這份清單記錄「Stage 8 正式上線後」要回頭處理的事項。
> 開發階段不做(會打斷主軸),上線後集中處理。

---

## 0. hotfix 2026-06-11 已結案(P0 美股修復 + P1 體檢清理)

> 任務書:`hotfix_2026-06-11_spec.md`｜回顧:`hotfix_2026-06-11_retrospective.md`
> commits:`e8ff004`(P0 碼)`6e77e20`(P0 重render)`c9d8ed7`(P1 碼)`03570a8`(P1 發布)

- ✅ ~~美股 chart 停產(404)~~ — 移除 last_bar==date 新鮮度閘(`e8ff004`)
- ✅ ~~國際股關鍵價遺失~~ — chart 層 fallback 讀 config/key_prices.json(`e8ff004`)
- ✅ ~~未收盤半成品 K 棒污染~~ — import REPLACE + 非台股重抓最近 N 根(`e8ff004`)
- ✅ 新增美股 05:30 補跑排程(`run_us_refresh.sh` + plist,06-12 自然驗收通過)
- ✅ ~~本節 1a 資料新鮮度 watchdog~~ — 已實證會響(`tools/test_watchdog.py`,`c9d8ed7`)
- ✅ ~~本節 1c tv_collect timeout~~ — 單檔 60s 逾時(`c9d8ed7`)
- ✅ 六個體檢 bug(history 無摘要 / ETF 空白名 / 紅漲綠跌 / 檔數 / 版本 / 更新日)
- ✅ site_meta 渲染單一資料源 + verify_publish 發布後線上驗證
- ✅ **v1 舊管線已停產 + 已封存**(2026-06 從 run_all.sh 移除 v1 三步;
  2026-09-18 將 `src/` 的 13 個 v1 py 檔 `git mv` 至 `archive/v1/`,
  另 2 個一次性 TV 除錯 mjs 移至 `archive/tools/`。封存前從排程進入點追依賴確認無活檔引用,
  封存後實跑 v2 主鏈/週報鏈/手動工具全綠。還原方式見 `archive/README.md`。
  `docs/` 下 v1 html 快照仍保留;`dashboard.html` 的 deprecated 註記不變。
  副作用:`tools/compare_v1_v2.py` 隨 v1 封存失效,未移,要用需先還原 v1)
- ✅ **MAERSK 代號結案**:維持 `OMXCOP:MAERSK_B`(TV 有效代號,非 typo),
  `watchlist.json` 已加 `_note` 鎖定,不改名

仍未做(本節 1 其餘):1b `tv-cdp.plist` 定時 restart、1d 評估換 K 線來源。

---

## 1. 重新審視自動化流程(2026-05-31 用戶提醒)

**用戶原話**:
> 「上線後記得提醒我要重新審一次自動化流程,
>   舊網站的自動化到後面沒辦法自己更新」

### 已知症狀(2026-06-01 凌晨深入確認)
- `kline.db` 最新只到 **2026-05-20**,5/21 ~ 5/31 共 **11 天沒新資料**
- **真因**:`tv_collect.mjs` 連 `data.tradingview.com/ping` 失敗,TradingView 內部 API
  session 失效
- Discord 通知機制**正常**:用戶 11 天都看到失敗訊息(`daily_supervisor` 健全)
- 排程本身**正常**:launchd 每天有觸發 `run_all.sh`,自動化骨架沒壞
- 卡的是 **5B 的資料源**(TradingView CDP)

### 為什麼會這樣
- `tv-cdp.plist`:`RunAtLoad=true, KeepAlive=false` → TradingView Desktop 一旦被關掉
  就不會自動重開
- `tv_collect.mjs` 連 TV API 失敗時**沒 retry 上限** → 5/31 跑 5+ 小時 retry,
  最後被用戶手動 kill
- 沒有「資料新鮮度」watchdog,只有「跑成功/失敗」狀態(失敗了但隔天又跑,
  狀態看起來「ok」卻沒新資料)

### 上線後要做的事
a) **資料新鮮度 watchdog**:檢查 `kline.db.MAX(date)` 跟今天差幾天,
   N 天沒新資料就告警(跟「跑成功/失敗」是不同訊號)
b) **`tv-cdp.plist` 改成定時 check + restart**(不只 login 時跑一次),
   確保 TradingView Desktop 一直在
c) **`tv_collect` 加 timeout 限制**(避免無限 retry 吃 CPU)
d) **或乾脆換掉 TradingView Desktop CDP**,改用其他 K 線來源
   (TWSE/TPEX 官方 API、yfinance、券商 API …)

### 用戶決策(2026-06-01)
- 「先不修,留給上線後重做」
- 開發階段繼續用 5/19 + 5/20 既有資料做 demo

**預估時間**:1-2 個工作日(深入排查 + 補強 watchdog + 評估換源)

---

## 2. 00403A 歷史補抓

- **00403A 上市**:2026-05-12
- **加入追蹤**:2026-05-31(用戶手動改 `~/ETF追蹤/daily_update.py:14` 加進 list)
- **缺口**:2026-05-12 ~ 2026-05-30 共 19 天 operations 漏抓
- **影響範圍**:5/12 ~ 5/30 的 `chip_etf` 計分(本來 6 檔精選池,實際只算 5 檔)
- **歸屬**:5A `daily_update.py` 一次性歷史抓取,**非 Stage 8 範圍**
- 用戶要做:跑歷史 backfill 把這 19 天補回去

---

## 3. ETF 範圍 review 機制

- **目前 6 檔精選池**:00981A / 00987A / 00992A / 00994A / 00995A / 00403A
  (基於 2025 年 ETF 績效前 5 名 + 00403A 新上市加入)
- **每年 1 月**依前一年績效 review 一次,汰弱換強
- **朋友 review 時建議**:加 2026-Q1 / Q2 季中進度檢視,避免一年只動一次
- 規則文件 §1-A 已明確寫「設計哲學:跟著贏家走,不跟著平均走」+ 共識窗口期 7 日

---

## 4. L1 sector 命名對齊(2026-06-09 標籤系統時記下)

### 現況
- `config/subtags.json` L1 用研究 Project markdown 命名(17 個 sector)
- `config/watchlist.json` 用 18 個 sector(IP與ASIC / 光通訊 / PCB / ABF 拆開)

### 不一致 3 處
| markdown(subtags.json L1) | watchlist sector |
|---|---|
| IP/ASIC | IP與ASIC |
| 光通訊/低軌衛星 | 光通訊 |
| PCB/ABF | PCB + ABF(2 個) |

### 影響
- ✅ 不影響排行(排行只用 L2/L3/L4,L1 是展示用)
- ✅ 後端計算不影響
- ⚠️ 影響「按 L1 板塊 filter / 顯示」功能(現在沒這個功能,所以還 OK)
- ⚠️ 未來 tags.html / dashboard 加「按板塊統計」要先對齊
- ⚠️ Watchlist 個股卡如果要顯示 L1 標籤,要對齊一致

### 觸發對齊的條件(任一)
1. 開發「按 L1 板塊 filter」功能時
2. 開發「板塊漲幅統計」功能時
3. Watchlist 個股卡顯示 L1 標籤時
4. 任何「混用 subtags.L1 跟 watchlist.sector」的功能

### 對齊方案(觸發時做)
建議 Option A:改 markdown,對齊 watchlist
- 「IP/ASIC」→「IP與ASIC」
- 「光通訊/低軌衛星」→「光通訊」
- 「PCB/ABF」→ 拆 PCB / ABF(朋友 review 個股歸哪個)

理由:
- watchlist 是 production 用,改動風險高
- markdown 是文字檔,改動風險低
- 朋友 review 過,可再 review 一次拆 PCB / ABF

工作量:10-15 分鐘 + 朋友 review 拆 PCB / ABF

---

## 5. stage9 新功能待決(2026-07-05 結案時記)

### 5-1 新聞功能兩待決方向（UI 改版季拍板）
> 現況:每日總經橫條已有「新聞關鍵字」列(`config/news_keywords.json` 外部化,
> 讀取已加防呆——解析失敗沿用上一份有效清單 + Discord 告警)。以下兩方向並非互斥,可並存:
- **方向 A**:關鍵字/新聞移入「入口頁」當小功能(現在掛在每日儀表板橫條,未來可能更適合放 landing)。
- **方向 B**:網站顯示新聞「標題 + 連結」,不轉內文(避免版權/爬文負擔;點連結回原站)。
- 兩者可並存;**等 UI 改版季一起拍板**,現在不動。

### 5-2 籌碼區欄位擴充（待朋友 review 確認）
> stage9 §3.5 籌碼面前半已上線:個股卡 K 線下方近 20 日 外資/投信買賣超直方圖 +
> 融資餘額 + 千張大戶比 + 投信買超 K 線橘點標記(純顯示,不進積分/排行)。
- **待朋友四合一 review 當面確認**:「籌碼區還想看什麼欄位?」(例:借券賣出、當沖比、
  主力進出、券資比…)——併入四合一 review(四個新功能一次驗),不隔空傳話。

### 5-2b 外資台指期未平倉淨口數 vs 加權指數 圖表(2026-07-07 拍板記入)
> 定位:**檔位模型 F1 訊號的視覺層**,待檔位模型立項一併實作。
- 資料:期交所官方「三大法人期貨未平倉」(外資台指期淨口數),fetch_taifex 進 19:00 主跑
- 回補:一年歷史入 macro.db(新表)
- 呈現:雙軸圖(外資淨口數 vs 加權指數),頁面位置屆時定
- 不先做:單獨圖表無決策掛鉤,等 F1 訊號規則(檔位模型)一起上,避免裝飾性圖表

### 5-2c 融資脈絡字「5日連增·近一年百分位」(2026-07-07 仲裁後列入)
> 交接包 §6 設計項;git 取證確認**從未實作**(7/5 交付=日變+週 WoW,非此項),非回歸。
- 前提:需**每日**融資餘額歷史序列(現只有 margin_weekly 週頻 1 列)
  - 新表 macro.db `margin_daily`;fetch_macro 每日順手寫入(+1 行改動)
  - 一年回補:MI_MARGN OpenAPI 只回最新日 → 需 spike TWSE rwd 歷史端點(MI_MARGN?date=)可行性
- 計算:連增/連減天數(streak)+ 近一年百分位;寫進 macro.json margin 欄(schema 增欄,需拍板)
- 呈現:§6 橫條融資格脈絡字(前端已留位)

### 5-3 事件中樞擴充(2026-07-06)——兩項待決
> 已上線六類新事件:台指期結算、央行理監事會、月營收截止(A 批純規則/config);
> 除權息(個股卡 💰 徽章)、台灣 CPI、台灣出口(B 批)。密度控制:同日合併 + >20 條只顯示中高。
- **國際股財報日:明確不做**——等朋友表態要不要(併入四合一 review)。
- **CPI/出口 config 校正**:`config/tw_macro_dates.json` 目前是「標準發布日推估」(CPI 每月5日、
  出口每月8日,遇假順延),**官方無機器可讀年度行事曆**;待朋友 review 時對主計總處/財政部
  官方月度公告校正。重要度預設(結算/除權息=中、央行/CPI/出口/月營收=中高)朋友 review 後可調。

---

## 6. 審計 2026-07-07 歸檔(來源:docs/audit_2026-07-07.md;速效包已處理 W1/W2/W3/D1/D2/D3)

> P0/P1 與速效項已於「正確性速效包」完成(2026-07-07/08,commits bd54cf1~360c9e5)。
> 以下 P2 與長期項逐條歸檔,標審計來源與嚴重度;動工前先讀 audit 原文對應段落。

### 6-1 P2 偏離項(D4~D10)
| # | 嚴重度 | 內容 | 修法方向 |
|---|---|---|---|
| D4 | P2 | 站穩取消未寫 breached_date(v2.2 §3-A 明定欄位缺席,失效日無從稽核) | schema 加欄 + CANCELLED 時寫 today |
| D5 | P2 | fetch_macro stooq 備援把「盤中漲跌」(close vs 當日開盤)冒充「日對日漲跌」 | stooq 路徑取近兩日收盤,或明標「盤中變動」 |
| D6 | P2 | fetch_chips 來源自報日 ≠ DATA_DATE 仍入庫標今天(舊值冒充) | 日期不符 → 該來源當日 N/A 不入庫 |
| D7 | P2 | 「絕不用舊值冒充」文件措辭與 spec 明訂的 stale 例外矛盾(code 合規,文件錯) | retrospective/待辦 文件補「除 §2.1 第四道護欄明訂之保留舊值」 |
| D8 | P2 | verify_publish 放行標準三套並存(chips/news 軟放行、weekly 硬 fail)無總表 | 補一張「哪些斷言不擋發布」總表進 verify_publish docstring |
| D9 | P2 | weekly 閾值宣稱全讀 config,code 內嵌同值 fallback(config 缺 key 靜默用內嵌) | 缺 key → 告警不靜默 |
| D10 | P2 | 文件引用失效(協作守則引用不存在的檔)+ v2.2 仍標「草稿」+ v1 遺留(signal_state.json/.bak 追蹤) | 文件清理一輪 + v2.2 請用戶升 final |

### 6-2 長期項
| 項 | 定位 | 內容 |
|---|---|---|
| **TV → 官方 API 遷移** | **stage11(UI 之後)** | 台股走 TWSE/TPEx 官方全市場日行情、美股 yfinance;先做「價格基準比對」(還原權息)+ 並行 5 交易日。audit 題一 W1 附表:4–5.5 工作日。300 檔擴張的硬前置(串行 CDP 27.5 分撞 30 分 timeout) |
| chart JSON 出 git | 擴張前 | 去 indent 已做(W3);下一步 orphan branch 單 commit force-push 或獨立 data repo(audit W3:現制 git 歷史 +1.5GB/年) |
| standing 狀態重建工具 | 擴張前 | 用每日 filtered_result 的 key_prices_snapshot 序列決定性重建 standing_state(消滅「孤本狀態」;audit W2 緩解③) |
| 跨日缺口自動補放 | 擴張前 | 單檔 K 棒缺口 ≥2 日自動逐日 replay 該 symbol(audit W2:靜默缺口改寫狀態機結果) |
| verify_publish 抽查去硬編碼 | 擴張前 | CHART_SAMPLES 寫死三檔,改 _index.json 分層隨機抽(audit 題三 #5) |
| watchlist 頁搜尋/篩選 | 擴張前 | 300 張折疊卡肉眼不可用(audit 題三 #6) |
| freshness watchdog 假日誤報 | 衛生 | 長假(春節)日曆日差 3 天連續誤報;改用 tw_holidays.json 算交易日差 |
| 週五美股收盤週末不入庫 | 衛生 | us_refresh 只排週一–五;遷 yfinance 後順手排週六 05:30 |
| filtered_result version 硬編碼 | 衛生 | metadata version "2.1" 與規則 v2.2 不同步(run_filters_v2.py:896) |

### 6-3 快照斷點紀錄(已執行,此處留檔)
- **6/15~7/2 停更回補批**(13 檔,commit 8369116):快照頁內 banner + history 列表標注
  「事後重算(以當前 config 重放,非當日等價)」——2026-07-08 標注升級完成。
  7/3 快照為 7/4 恢復日自然跑產出(c4c65ce),不屬回補批、不標注。
- **D1 區域計分斷點**:2026-07-08 起對齊 v2.2 §3-B,此前依舊制。**不標注、不重算**,
  斷點以 commit f2b96ca 與 audit 文件為準(用戶拍板:兩者不要混)。

---

## 7. 資料同步技術債(2026-09-06 關鍵價上架期間暴露,另立任務處理)

> 三條都是「手動同步/中間層」造成的靜默出錯風險。現況都有「擋著的防線」,
> 但防線是治標,根本問題待解。動工前先讀本節。

### 7-1 key_prices md 中間層名存實亡 → ✅ 已解(2026-09-18 採方案 a:廢中間層)
- **原問題**:`key_prices_clean_v3.md` 只有 112 檔,權威真值 `config/key_prices.json` 有 140 檔;
  md 缺 28 檔、32 檔手改股與 JSON 分岔。任何人重跑轉換器(md→JSON)會把 140 檔覆蓋成殘缺版。
- **處置(採方案 a,廢掉中間層)**:
  1. `key_prices_clean_v3.md` + 5 個 .bak → `git mv archive/md-layer/`(封存,非刪除)。
  2. `tools/convert_key_prices.py` 移除 DEFAULT_MD、`--md` 改必填 → 無參數重跑會提示必填、
     不再去讀那個 md。上架流程正式定為:判讀批 → 小 md → convert 產片段 → 拼接進 JSON。
  3. `config/key_prices.json` 為**唯一權威真值**(前端/計分本就只讀它)。
  4. **防重跑保護保留**(2026-09-06 `e5adbcf`):它保護 --out 目標 JSON,與 md 封存無關;
     任何時候用小批 md 跑仍可能誤覆蓋 140 檔,這道防線續留。
- **驗證**:封存後 convert dry run 無參數→提示必填(非崩潰)、真批 md→正常轉;
  config/key_prices.json 140 檔完好;主跑排程完全不碰 convert/md。還原見 archive/README.md。

### 7-2 visual.json 雙檔手動同步
- **問題**:`config/visual.json`(原始檔,沒人讀)與 `docs/data/v2/visual.json`
  (前端實際 fetch 的檔)靠手動同步,且已分岔——docs 版多了 etf_arrow_buy/sell、
  chip_trust_buy 三鍵;config 版格式/updated_at 也不同。**改 config 前端吃不到**
  (2026-08 破壞塊配色事件即因此:改 config 沒反應,實際要改 docs)。
  反向若用 config 覆蓋 docs 會掉那三個 ETF/投信鍵 → 箭頭顏色靜默壞掉。
- **現在靠什麼擋著**:無自動防線,純靠「記得改 docs 版、且只加不覆蓋」。
  2026-09-06 起本輪改動都直接改 docs 版並保留獨有鍵。
- **未來怎麼根治**:① 認定單一真值 + 在 publish/prepare_charts 自動 `cp` 同步
  (先把兩版合併成完整超集當基準);② 或在 verify_publish 加「config vs docs
  visual.json 不一致就 Discord 告警」的斷言。建議 ① 治本 + ② 保險。

### 7-3 手改股 md↔JSON 不一致(7-1 的子集,獨立列供追蹤)
- **問題**:約 42 檔手動維護股(記憶體群/三星/SK/上銀/研華/廣達撐轉/可成破壞塊…)
  的關鍵價是直接在 JSON 手改或來源不明,md 表達不全或缺塊。這是 7-1「md 缺 28 檔 +
  32 檔分岔」的具體成員。
- **現在靠什麼擋著**:同 7-1 的防重跑保護(不讓殘缺 md 覆蓋)。
- **未來怎麼根治**:併入 7-1 一起解——若採 7-1(a) 廢 md 層,這 42 檔就以 JSON 為準、
  問題消失;若採 (b) 重建同步,需逐檔把手改內容補回 md 或修轉換器使其可表達。

---

*整理:Claude Code(2026-06-01,Stage 8 W3 demo 結案後;2026-07-05 補 stage9 待決;2026-07-08 補審計歸檔;2026-09-06 補 §7 資料同步技術債)*
*狀態:⏳ 上線後 / 觀察期處理*
