# 系統現況快照(2026-06-02)

## 上線狀態

| 項 | 值 |
|---|---|
| 線上 URL | https://mardichao-dotcom.github.io/daily-stock-analysis/ |
| 規則版本 | **v2.2**(2026-06-02 上線) |
| watchlist 規模 | **87 檔**(台股 58 / 16 板塊 + 國際 29 / 6 族群) |
| 計畫擴增目標 | 台股 200 + 美股 100 |
| 測試 | 397 tests / OK |

## 今日(2026-06-02)完工

### 朋友規則 v2.2 實作
- §1-D 區域觸發改 K 棒交集(`K_low ≤ high AND K_high ≥ low`)
- §3-A 站穩 / 跌破 2 天確認 + leave_up cancel(雙向取消)
- §3-A 跌破改 event-based,不需 prior STANDING state
- §3-B 區域類碰到當天 +N(不需 2 天確認)
- §4 C 級分組只列「當天新成立」(tags_today)
- `commit c7aa74e + b692954`(scoring + tests)

### UX 改進
- 6862 三集瑞-KY 名字修正(從錯誤的「勝麗」)
- 前十名升級為折疊卡片(🥇🥈🥉 + chart placeholder,`f4fc2e5`)
- 美股時差 amber 等待色塊(`ba4ac04`):
  「⏳ 等待 US 收盤資料 / 最新資料:2026-05-29」
- 雙頁互連 nav + 響應式

### 自動化
- TradingView 開機自動啟動(`com.user.tradingview-cdp`,KeepAlive=true)
- 舊 `com.stock-dashboard.tv-cdp` 已 disabled
- 永久解放 terminal(用戶不需手動維持 TradingView)

### 歷史補完
- 5/21~6/01 連續 10 天 snapshot 全 render(`dd996b0` + 後續 v2.2 重算)
- 6862 在 5/21/22/27 進 A 級+(修名後正確顯示)

---

## 自動化排程

| 排程 | 觸發時機 | 內容 |
|---|---|---|
| `com.stock-dashboard.run-all.plist` | 每天 19:00 | tv_collect → daily_update → import_kline → run_filters_v2 → prepare_charts → render → publish |
| `com.user.tradingview-cdp.plist` | 登入時 + KeepAlive | 保持 TradingView Desktop + CDP port 9222 在線 |

| 監控檔 |
|---|
| `state/automation_status.json` daily_supervisor 寫的狀態 |
| `~/Library/Logs/tv-cdp.log` TradingView 自身 debug 訊息 |
| `~/Library/Logs/tv-cdp.err` 含 `DevTools listening on ws://127.0.0.1:9222/...` |
| `logs/run_all.log` 每日總管 log |

### 資料源(唯一合法來源)

| 來源 | 路徑 |
|---|---|
| K 線歷史 | `kline.db`(local SQLite,由 tv_collect 寫) |
| ETF 動作 | `~/ETF追蹤/etf_operations.db`(由 daily_update.py 寫,5A) |
| 6 檔 ETF 精選池 | 00981A / 00987A / 00992A / 00994A / 00995A / 00403A |

---

## 待辦(優先序)

### 高優先
- [ ] `add_symbols_batch.sh`:批次加新 symbol 到 watchlist + 補 K 線歷史
- [ ] 增量跑邏輯:tv_collect 只抓需要的天數,避免無謂重抓
- [ ] `tv_collect timeout` 評估:200 檔擴張前確認跑得完 30 min

### 中優先
- [ ] `watchlist_v2.html` 200 檔效能評估(目前 87 檔 OK,3.7MB chart data;200 檔約 9MB)
- [ ] 美股凌晨 04:30 launchd(額外排程補美股盤後資料)
- [ ] daily_supervisor 加 fresheness Discord 告警(已有 watchdog logic,需確認觸發)

### 低優先 / 後續
- [ ] 朋友手繪 30 檔 key_prices / 週 → 5 週擴完 200 檔台股
- [ ] watchlist_v2 加搜尋 / 篩選功能
- [ ] 個股輪動 emoji 視覺強化

---

## 朋友協作節奏

- 朋友每週手繪 **30 檔 key_prices**
- 預計 **5 週**擴完 200 檔台股
- 每週 review 規則調整(2026-06-02 升 v2.2 後若還有發現再調)

---

## 給未來 Claude 的快速 onboarding

1. 讀 `朋友規則_v2_2.md`(業務邏輯)
2. 讀 `docs/AI協作守則.md`(怎麼跟 Claude 互動)
3. 看本文件(最新狀態)
4. 跑 `git log --oneline -10` 看最近改動
5. 跑 `python3 -m unittest discover -s tests 2>&1 | tail -3` 確認 397 tests 全綠

---

*整理:Claude Code(2026-06-02 凌晨)*
*下次更新:朋友規則再升 / 上線後重大事件 / 自動化失效時*
