# ── 資料庫（絕對不上傳）────────────────────────────────────────────────────
# kline.db / etf_operations.db / test_kline.db 一律存本機
*.db
*.sqlite

# ── 測試資料目錄 ─────────────────────────────────────────────────────────────
test/

# ── 本機開發產出（不追蹤；部署版本在 docs/）──────────────────────────────
/output/
/data/

# ── Python 暫存 ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble

# ── 敏感設定 ─────────────────────────────────────────────────────────────────
.env
*.local
key_prices.txt
config/secrets.json

# ── 執行階段產物（不進 git）──────────────────────────────────────────────
state/automation_status.json
.data_date
logs/

# ── 未來可能的暫存檔 ──────────────────────────────────────────────────────
tv_daily_data.json
/tmp/

# ── 開發專用文件（不納入 portfolio repo）────────────────────────────────
stage*.md
!docs/stage*.md
chart_test.html
積分制定案_v1_正式版.md

# ── Stage 8 執行階段產出(可由腳本重產生)─────────────────────────────
filtered_result*.json
*.bak
# 歷史 chart JSON(每天 ~3.7MB × 87 檔,可由 prepare_charts_v2 重產)
docs/data/v2/2[0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
config/.news_keywords.last_valid.json
design/
handoff/
台股盤後決策儀表板改版.zip
